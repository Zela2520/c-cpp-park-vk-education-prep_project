#include "../../include/danya_client.h"

#define MAX_CLIENT 2
#define OBJECTS 1

//Client::Client(){
//    //// инициализируем героев
//    sf::Texture amogusTexture;
//    amogusTexture.loadFromFile("../include/textures/amogus.png");
//    for (size_t i = 0; i < MAX_CLIENT; ++i) {
//        m_players.emplace_back(Player(0, 0, amogusTexture));
//    }
//    //// инициализируем неподвижные объекты
//    sf::Texture gachiTexture;
//    gachiTexture.loadFromFile("../include/textures/gachi.png");
//    for (size_t i = 0; i < OBJECTS; ++i) {
//        m_unmovables.emplace_back(Unmovable(200, 200, gachiTexture));
//    }
//    //// инициализация моба
//    sf::Texture mobTexture;
//    mobTexture.loadFromFile("../include/textures/tnt.png");
//    // m_mob = Mob(-500.0, -500.0, mobTexture);
//    m_mob.setScale(0.8,0.8);
//}

